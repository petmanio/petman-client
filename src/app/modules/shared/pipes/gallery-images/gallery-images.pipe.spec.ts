import { GalleryImagesPipe } from './gallery-images.pipe';

describe('GalleryImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new GalleryImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
